{
  "name": "chache_generate",
  "version": "1.0.0",
  "description": "Create a JSON with the file for chaching using in the PWA ServiceWorker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Benjamin Brodwolf",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
